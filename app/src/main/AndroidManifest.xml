<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.packetsniffer">
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_INTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Packetsniffer" >
        <activity
            android:name=".Views.PacketInfoActivity"
            android:exported="true"
            android:label="@string/title_activity_packet_info"
            android:theme="@style/Theme.Packetsniffer.NoActionBar" />
        <activity
            android:name=".Views.PacketListView"
            android:exported="true" />
        <activity
            android:name=".RTTGraphActivity"
            android:exported="true" />
        <activity
            android:name=".Views.MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>